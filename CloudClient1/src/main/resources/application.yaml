server:
  port: 0
spring:
  application:
    name: client1
  cloud:
    discovery:
      enabled: true
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka